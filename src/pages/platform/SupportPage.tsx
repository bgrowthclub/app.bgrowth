import PlaceholderPage from '../../components/platform/PlaceholderPage'

export default function SupportPage() {
  return (
    <PlaceholderPage
      title="Support"
      description="Get help from the BGrowth team — support's home inside the platform."
      path="/platform/support"
    />
  )
}
