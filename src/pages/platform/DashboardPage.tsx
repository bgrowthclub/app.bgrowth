import PlaceholderPage from '../../components/platform/PlaceholderPage'

export default function DashboardPage() {
  return (
    <PlaceholderPage
      title="Dashboard"
      description="Your home base across every BGrowth product — a snapshot of your Business Systems, activity, and next steps."
      path="/platform/dashboard"
    />
  )
}
