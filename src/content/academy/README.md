# content/academy

Reserved for future course content (the BGrowth Academy™ ecosystem
product — see PRODUCT_CATALOG.md, ARCHITECTURE.md §10).

Empty on purpose. No types, no data, no logic — this phase only
establishes `products/` and `shared/`. This folder is scaffolded now so
the top-level `content/` structure is visible, not because course content
types are defined yet.
